name: CI Java (Host)

on:
  push:
    branches:
      - "**"
    tags:
      - "!**"
  workflow_dispatch:

permissions:
  contents: read

jobs:
    build:
        name: build-test-deploy
        uses: ebpro/gh-actions-shared-java/.github/workflows/ci-shareable-maven.yml@develop
        secrets: inherit
        with:
          runs-on: ebpro-org
          jdk-matrix: '[ "25" ]'
          maven-matrix: '[ "3.9.11" ]'
          verify-goal: 'install'
          quality-goal-run: "false"
          deploy-goal-run: "true"
          deploy-goal-develop: ''
          deploy-goal-release: ''
          site-goal-run: "true"
          site-goal-branch: "master"
